
# Day planner
- [ ] 09:00 - 15:00 调试centos8 pagecache被覆盖问题
	- [X] 09:00 - 14:45 定位到原因，是因为xfs 有`xfs_file_aio_write_checks()` 函数，该函数会检测当前inode的大小和要写入off大小，而中间的gap `[inode->i_size, off)` 要被赋值为0, 而 `read_mapping_page()` 不会更改 inode大小。
    - [X] 14:46 - 15:40  在`init_super_block()`函数中将 `inode->i_size`设置为该文件除 **cow_bitmap** 之外的最大数量的块解决
    - [x] 15:41 - 17:00 解决 mount 场景下，`do_read_cache_page` 卡死问题 ✅ 2026-06-24