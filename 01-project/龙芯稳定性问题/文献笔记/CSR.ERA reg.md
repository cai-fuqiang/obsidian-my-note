> [!PDF|note] [[龙芯架构参考手册卷一_r1p10.pdf#page=132&selection=198,0,202,4&color=note|📖CSR.ERA 存储触发异常PC的异常类型(其他类型不写 CSR.ERA)]]
> > 该寄存器记录普通例外处理完毕之后的返回地址。**当触发例外时，如果例外类型既==不是 TLB 重填例外==，也==不是机器错误例外==，则==触发例外的指令的 PC== 将被记录在该寄存器中**。
