以 `ALCL0`为例
> [!PDF|] [[龙芯架构参考手册卷一_r1p10.pdf#page=130&selection=28,0,33,8|📖 ALCL0]]
> > PLV0 特权等级下，是否对允许非对齐的非向量 load/store 指令1进行非对齐检查。为 1 表示进行检查，如果违例则触发地址对齐错例外。**该位仅当硬件实现==支持==这些非向量 load/store 指令地址==非对齐==时才是==可读写的==，否则该位只读恒为 1**
